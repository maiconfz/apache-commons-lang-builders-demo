# apache-commons-lang-builders-demo
Just a project to test the Builders from Apache Commons Lang Builders at the package "org.apache.commons.lang3.builder"

You can run the project using `./gradlew run`